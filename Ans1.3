1)pwd-full name (print working directory) writes the full pathname of the current working directory to the standard output.

2)vi-full name(visual editor) The UNIX vi editor is a full screen editor and has two modes of operation:
1)Command mode commands which cause action to be taken on the file
2)Insert mode in which entered text is inserted into the file.

3)touch-The touch command is the easiest way to create new, empty files.
It is also used to change the timestamps (dates and times of the most recent access and modification) 
on existing files and directories.

4)mkdir-full name(make directory) directory name is the name of any directory that the user is asking mkdir to create.
Any number of directories can be created simultaneously.

5)rm-full form(remove) used for remove directory.

6)ls-full form(list directory contents) gives the list of contents present in current directory.

7)echo-echo is a fundamental command found in most operating systems that offer a command line. 

8)cat-This command is used for reading  the contents of files.

9)who-this command is used for display a list of users who are currently logged into the computer.

10)cd-The cd command, also known as chdir (change directory), is a command-line OS shell command used to 
change the current working directory in operating syste.

11)date-this command is used to set the system date and time.

12)cal-full form (calender) used  for a displays a calender.

13)mv-this command is used for move files and directories.

14)cp-this command is used for copying file and directories.

15)which-this command is used to identify the location of executables.
